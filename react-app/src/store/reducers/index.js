//Declare Initial State
const initialState = {
    pokemon: [],
    isLoading: false,
    error: ''
}

//Export Reducer Function
export const reducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state
    }
}